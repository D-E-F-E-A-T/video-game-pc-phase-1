#include "pch.h"

/**
  This represents the entire 2D world which is a grid of Screens
*/
class World
{
public:
	World();
	//void AddScreen(int horizontalOffset, int verticalOffset, Screen screen);
	//int MoveToScreen(int direction, Screen * currentScreen);

protected:

private:
};